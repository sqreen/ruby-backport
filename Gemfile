source 'https://rubygems.org'

gem 'rake', '~> 11.0' if RUBY_VERSION.start_with?('1.9.3')
gem 'rake', '~> 11.0' if RUBY_VERSION.start_with?('2.0')
gem 'rake', '~> 11.0' if RUBY_VERSION.start_with?('2.1')
gem 'parallel', '< 1.14' if RUBY_VERSION.start_with?('1.9.3')
gem 'parallel', '< 1.14' if RUBY_VERSION.start_with?('2.0')
gem 'parallel', '< 1.14' if RUBY_VERSION.start_with?('2.1')
gem 'rubocop', '~> 0.50.0' unless RUBY_VERSION.start_with?('1.9.3')

gem 'minitest', '~> 5.0.0'
gem 'pry'

gemspec
